<%= form_with(model: tax_category) do |form| %>
  <% if tax_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tax_category.errors.count, "error") %> prohibited this tax_category from being saved:</h2>

      <ul>
        <% tax_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <form class="row g-3">
    <div class="col-md-6">
      <div class="col-md-4">
        <label for="inputAddress" class="form-label">Tax Rate</label>
        <%= form.text_field :tax_rate, {class: 'form-control'} %>
      </div>
      <br>
      </div>
    <div class="col-12">
      <%= form.submit :class => 'btn btn-primary' %>
    </div>
  </form>
<% end %>
